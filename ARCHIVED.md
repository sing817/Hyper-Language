# Hyper-Language 專案封存說明

**狀態**：已放棄（Abandoned）  
**日期**：2026年5月

---

## 原始動機

這個專案的起點是想改善**小模型在多語言場景下的表現**。

核心觀察是：同一個概念（例如「蘋果」）在不同語言的資料中存在大量重複。如果能找到方法壓縮這些跨語言的重複內容，或許可以讓小模型用更少的資料和運算，學到更好的多語言能力。

最初的想法是：既然中文的資訊密度相對較高，或許可以把所有語言先轉換成以中文為主的「Hyper Language」中間表示，再餵給模型訓練，藉此達到壓縮重複、提升效率的目的。

## 實際嘗試的做法

專案後續發展成一個複雜的預處理 tokenizer（HL Tokenizer），主要流程包括：

- 使用 LLM（後期改用 Grok）進行語言標記與切分
- 使用 NLLB-200-distilled 大型翻譯模型將非中文內容翻譯成中文
- 搭配大量啟發式 fallback 機制
- 加入語言標籤（`[en]`、`[原]`、`[ja]` 等）保留可逆性
- 最終輸出類似 `[en][HL你好][/en][原][HL世界][/原]` 的格式

目標是實現「單次編碼、多語言轉中文、資訊密度最大化」。

## 後來發現的主要問題

經過實際實現與測試後，發現這個方向存在幾個根本性的限制：

1. **效率目標自相矛盾**  
   為了壓縮重複，卻引入了兩個重量級外部模型（Grok API + NLLB 1.3B），單句處理延遲經常達到數秒到十幾秒。這與「小模型追求效率」的初衷完全背道而馳。

2. **壓縮效果多為幻覺，伴隨資訊損失**  
   許多看似強大的壓縮案例，實際來自 NLLB 翻譯失敗後的粗暴 fallback 映射。大量語義細節被抹除，這對模型學習反而有害。

3. **把問題轉移而非解決**  
   這個方法沒有真正減少模型需要學習的內容，而是把跨語言對齊的難題，轉嫁到「高品質翻譯」這個更難的子問題上。

4. **在表示層的改進空間有限**  
   後續反思發現，小模型多語言能力不足，更主要的瓶頸在於**資料品質與分佈**、**訓練目標設計**、以及**模型架構對跨語言知識的共享方式**，而非單純的輸入表示轉換。

5. **難以規模化使用**  
   整個 pipeline 過度依賴外部付費 API 和大型本地模型，穩定性和實用性都很差。

## 最終結論

這個特定「以中文為高密度樞紐語言進行預處理壓縮」的技術路線，經過驗證後被認為**不適合繼續發展**。

雖然「減少跨語言概念重複、提升小模型多語言效率」這個問題本身仍然值得研究，但更好的攻擊點應該放在資料策展、合成資料、跨語言對齊目標、以及模型架構層面，而不是在 tokenizer / 輸入表示層做這類高成本、有損的轉換。

## 為什麼保留這個儲存庫？

保留此 repo 是為了：

- 記錄一次完整的探索過程
- 讓後來對類似想法有興趣的人，可以看到實際嘗試後遇到的具體問題
- 避免其他人重蹈相同的覆轍

---

**本專案已不再維護，也不會再有更新。**

如對小模型多語言效率議題感興趣，建議關注更主流且有實證效果的方向（高品質多語言資料建構、continued pretraining 食譜、跨語言對齊方法等）。
